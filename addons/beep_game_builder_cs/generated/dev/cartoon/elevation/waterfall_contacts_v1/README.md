# Granite waterfall contacts

New green-backed source candidate, generated with the built-in image tool.
`sources/contacts_source_v1.png` contains six detached modules: left/right wall
contacts, lip ends and bottom contacts. `specification.json` declares target
runtime bounds, pivots and protected water half-planes before calibration.

The generator could not read the local reference through its filesystem sandbox.
The source was therefore generated from a textual description after inspecting
the existing front-wall source, not as a pixel-preserving reference edit.

The straight water-facing edges are present. Lip and footing proportions still
need crop/scale calibration; this image is not a validated atlas. Source appearance
approval has been requested and is not assumed. No runtime scene uses this source.
No existing artwork or animation was replaced. Isometric contacts need separate art.

Generation prompt: `prompt.md`.
