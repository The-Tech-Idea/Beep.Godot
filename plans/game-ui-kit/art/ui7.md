# `ui7.png` — **byte-identical duplicate of `gameui9.png`**

**649,932 bytes · MD5 `eb7314cc4161bb513eedd7b4960678be`** — the same hash as
`gameui9.png`.

```
gameui9.png  eb7314cc4161bb513eedd7b4960678be
ui7.png      eb7314cc4161bb513eedd7b4960678be
```

The two files are the same image: the pixel-art survival crafting screen.

**See [gameui9.md](gameui9.md)** for the full measured document — diegetic workbench panel,
hotbar selection at a 3px white outline, recipe board grid at 1.20 × the fill, the
quantity-subscript pattern, and selection-by-elevation on the tab strip.

---

## Consequence for the folder

`Example_Art/` contains **44 files but 43 unique images**. Hashes were checked across the
`ui*` and `gameui*` sets; only this pair collides.

Recorded so a future pass does not spend a second document on it, and so any "N images
reviewed" count is honest.

## Action

- [ ] Consider deleting `ui7.png` from `Example_Art/`, or leave it and rely on this note.
      Either is fine; what matters is that the duplication is known.
